# Multa — Архитектура

## Стек (решение)

Монорепо pnpm workspaces + Turborepo:

```
multa/
  apps/
    web/        — Vite + React 18 + TypeScript + TanStack (Router, Query) + Tailwind
    api/        — Node 22 + Hono + Drizzle + better-auth. Docker, свой сервер
    bot/        — grammY, webhook. Фаза 2
  packages/
    core/       — чистый TS без зависимостей: money, fx, cascade, periods, forecast, parser
    api-client/ — типизированный клиент + zod-схемы (общие для web/bot)
    i18n/       — словари ru/en, типобезопасные ключи
  docs/         — этот пакет документации
  docker-compose.yml — postgres, api, caddy
```

- **Хостинг: свой сервер (решение основателя), Docker Compose**: Postgres 16 + apps/api (Node 22 + Hono) + Caddy (TLS/reverse proxy) + том для файлов чеков (или MinIO, если захочется S3-совместимости). Никакого BaaS-аккаунта.
- **Доступ к данным: только через API.** Клиент никогда не ходит в БД напрямую. Изоляция workspace — middleware в API (каждый запрос скоупится по workspace_id владельца токена); опционально RLS в Postgres как вторая линия обороны.
- **ORM/миграции**: Drizzle (SQL-first, отличная работа с bigint/numeric).
- **Auth**: better-auth (TS, self-hosted): email magic link + Google OAuth. Telegram Login добавляется в фазе бота (маппинг tg_user_id → user).
- **Cron**: pg_cron в Postgres или node-cron в API-процессе (fx-пайплайн, сверка балансов, дайджесты).
- **Веб**: SPA + PWA (manifest, service worker, очередь офлайн-мутаций: записал расход без сети → синк). SSR не нужен; лендинг — отдельная статика.
- **i18n**: ru/en с первого коммита. Ключи типа `plan.rebalance.title`, никакого хардкода строк в компонентах.

## packages/core — сердце системы (максимум тестов здесь)

- `money.ts` — Money в integer minor units, exponents по ISO 4217 (JPY=0, BHD=3), convert() с RateSnapshot. Float в деньгах запрещен.
- `cascade.ts` — распределение выплаты по приоритету (debts → buckets → envelopes → categories → goals) и обратное сжатие при нехватке. Чистая функция: (income, plan, history) → allocation | suggestions.
- `periods.ts` — генерация PayPeriods из якорей («10 и 25», «каждые 2 недели»), привязка транзакции к периоду.
- `burnRate.ts` — темп расхода категории, прогноз даты исчерпания, порог сигнала.
- `forecast.ts` — cash-flow на N месяцев: recurring + debts (с датами закрытия) + buckets + goals.
- `parser.ts` — свободный текст/транскрипт голоса → {amount, currency?, categoryHint?, itemName?, date}. Сначала regex-эвристика, LLM — фоллбек на сервере. Требования (acceptance-кейсы, обязательные тесты):
  - «250 еда» → 250, дефолт-валюта, Еда
  - «кофе 4.5 eur вчера» → 4.50 EUR, вчера, itemName «кофе»
  - «кофе капучино 0,3 400 динар» → 400 RSD, itemName «кофе капучино 0,3» — **не перепутать объем/количество с суммой** (эвристика: сумма — число рядом с валютой или последнее «денежное» число; 0,3 при слове-объеме — атрибут позиции)
  - «хлеб 200 молоко 150 такси 900» → три транзакции
  - валюта словами и склонениями: «динар/динара/дин», «евро», «рублей/руб», «лари», «бат»
  - числа: запятая и точка как десятичный разделитель, «1.5к»/«полторы тысячи» → 1500
  - неуверенность → не «не понял», а догадка с подтверждением: «400 RSD в Еду?» [да] [изменить]
  - itemName сохраняется в note/raw_input — из голосовых накапливается та же детализация, что из чековых позиций.
- `spread.ts` — фактический курс из пары сумм, спред к официальному.

## FX-пайплайн

1. pg_cron (ежедневно 06:00 UTC): ЦБ РФ `XML_daily.asp` (windows-1251! курс «на завтра»), Frankfurter (ЕЦБ) → upsert в `fx_rates`.
2. `getRate(base, quote, date, workspaceId?)`: fx_rates → ленивая дотяжка из API (исторические курсы вечны — кешируем навсегда) → выходные: последний рабочий день → нет данных: просим ручной ввод.
   **Личный курс важнее котировки на ту же дату.** Курс дня выплаты человек вводит руками, глядя на табло обменника (issue #48); он живёт в `fx_manual_rates` со скоупом воркспейса и подмешивается к котировкам только когда `getRate` знает воркспейс. Писать его в `fx_rates` нельзя: та таблица общая для всех, и личный факт протекал бы в чужие планы (правило 7). Это уже случалось: демо-сид писал свои курсы в `fx_rates` с `source='manual'`, и вход в демо переопределял курс реальным воркспейсам — теперь курсы демо тоже личные. Свежесть при этом важнее источника — вчерашний ручной курс не побеждает сегодняшнюю котировку.
3. Кроссы вычисляются, не хранятся.

## AI-слой (только API, без своих моделей)

| Задача                | Путь                                                                                              | Модель/метод                | Стоимость        |
| --------------------- | ------------------------------------------------------------------------------------------------- | --------------------------- | ---------------- |
| Чек РФ                | QR даёт только реквизиты; состав — из API приложения ФНС (авторизация по телефону, неофициальный) | без AI, но с оговорками     | free, ненадёжно  |
| Чек Сербия            | QR даёт ссылку; состав — со страницы проверки suf.purs.gov.rs (без авторизации)                   | без AI                      | free             |
| Чек прочее            | фото → vision structured output                                                                   | Gemini Flash / Claude Haiku | ~$0.001–0.01/чек |
| Текст «250 еда»       | regex → LLM-фоллбек                                                                               | Haiku                       | ~$0.0003/зап.    |
| Голос                 | Whisper API → текстовый пайплайн                                                                  | whisper                     | ~$0.006/мин      |
| Категоризация позиций | LLM few-shot историей юзера                                                                       | Haiku                       | копейки          |

Правила: confidence < порога → сумма в «Общее» одним тапом; в LLM не отправлять user_id и лишний контекст; выключатель AI в настройках; лимит бесплатных распознаваний = граница Free/Pro.

## Безопасность

- Изоляция workspace в API-middleware на каждом запросе; опционально RLS как вторая линия.
- Сессии better-auth: короткоживущие токены, refresh httpOnly. Webhook бота — secret_token + rate limit.
- Экспорт CSV/JSON и удаление аккаунта — с MVP (доверие + GDPR).
- Sentry без PII. Секреты в env. **Бэкапы: pg_dump ежедневно + копия вне сервера (rclone в облако), restore проверяется раз в месяц** — на своем железе это на нас.
- БД не смотрит в интернет: Postgres только в docker-сети, наружу — один Caddy.

## Инфраструктура и стоимость

- Свой сервер: стоимость железа/электричества; софт $0. AI-затраты $5–50/мес по мере роста.
- Публичный доступ: домен + Caddy (auto-TLS). Если сервер дома — Cloudflare Tunnel, чтобы не открывать порты.
- План Б при росте: тот же docker-compose переезжает на VPS (Hetzner €5–10) без изменений кода.

## Наблюдаемость

Sentry (web+api), структурные логи Hono, аудит plan_revisions как продуктовая метрика (сколько пересборок принято — ключевой KPI полезности).

## Профиль запуска: нулевая стоимость (решение основателя, действует для MVP)

Ни одного платного сервиса и хостинга на старте. Конкретно:

| Потребность                    | Решение за $0                                                                                                                                                                                                                            | Платный аналог — когда-нибудь потом |
| ------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------- |
| Хостинг                        | свой сервер, docker-compose                                                                                                                                                                                                              | VPS Hetzner                         |
| Доступ к вебу с телефона/извне | **Tailscale Funnel** (бесплатный план: публичный HTTPS-адрес `*.ts.net` без домена и без открытых портов; `serve` — то же самое, но только внутри tailnet)                                                                               | домен + Caddy                       |
| Telegram-бот                   | **long polling** (grammY) — не нужен ни домен, ни публичный URL, ни webhook                                                                                                                                                              | webhook за доменом                  |
| Auth                           | better-auth: логин+пароль + TOTP (включение с QR и резервными кодами, шаг проверки при входе); Telegram Login. **Без email-провайдера вообще** — magic links не нужны, пока пользователь один                                            | Resend/Postmark                     |
| Парсер ввода                   | только regex-эвристика из packages/core (покрывает «250 еда», «кофе 4.5 eur»)                                                                                                                                                            | LLM-фоллбек                         |
| AI чеков                       | **в MVP**: QR-путь (ФНС РФ / suf.purs.gov.rs) бесплатный первым; фоллбек — OpenAI API основателя (gpt-4o-mini vision, structured output; ~$0.001–0.01/чек). Внимание: нужен ключ platform.openai.com — подписка ChatGPT Plus API не дает | Gemini/Anthropic как альтернативы   |
| AI голоса                      | после MVP: Whisper API (тот же ключ OpenAI) или whisper.cpp на своем сервере                                                                                                                                                             | —                                   |
| Курсы валют                    | ЦБ РФ + Frankfurter (ЕЦБ) — бесплатны без ключей                                                                                                                                                                                         | openexchangerates                   |
| Мониторинг                     | Uptime Kuma (self-hosted) + docker-логи; Sentry заменяет self-hosted GlitchTip или просто пока без него                                                                                                                                  | Sentry SaaS                         |
| CI, репозиторий                | GitHub Free + Actions free tier                                                                                                                                                                                                          | —                                   |
| Бэкапы                         | pg_dump кроном + копия на второй диск/в TG-канал себе (шифрованный архив)                                                                                                                                                                | S3                                  |

Единственная неизбежная трата в будущем — домен (~$10/год). Пока не нужен и он: с 2026-08-02 продукт доступен в интернете через Tailscale Funnel по адресу `muspelheim.tail48dfee.ts.net` — бесплатно и без открытых портов. Домен понадобится, когда захочется человеческого имени и почты на нём, а не для доступа. Long polling закрывает бота.

## Production-блоки («взрослый» контур)

- **apps/admin** — админка (см. 09-admin.md): отдельный поддомен, admin_users + TOTP, аудит-лог.
- **Биллинг** — Stripe + Telegram Stars + comp из админки (см. 08-billing.md); entitlements-middleware в API.
- **Транзакционные письма** — managed-провайдер (Resend/Postmark, ~$0–20/мес): magic links, чеки оплаты, напоминания past_due, GDPR-экспорт. Self-hosted SMTP не берем — deliverability не наша война. Шаблоны в ru/en.
- **Окружения**: dev (docker-compose локально) → staging (тот же compose, отдельная БД) → prod. CI: GitHub Actions — typecheck, тесты, build, деплой по SSH (docker compose pull && up -d). Миграции — в CD-шаге, с бэкапом перед прогоном.
- **Rate limiting**: на auth-эндпоинты и AI-эндпоинты (per-user и per-IP), в API-middleware; Caddy — базовый лимит на всё.
- **Мониторинг**: Uptime Kuma (self-hosted) на публичные эндпоинты + healthcheck fx-пайплайна (свежесть курсов < 36ч); алерты в TG основателю.
- **Продуктовая аналитика**: своя таблица событий (event, workspace_id, props, ts) + дашборд в админке. PostHog self-hosted — если станет мало. Никаких сторонних трекеров на клиенте — это часть privacy-обещания.
- **Статус-страница**: простая (Uptime Kuma public page) — status.multa.*.
- **Версионирование API**: /v1/* с первого дня; bot и web ходят только через версионированные пути.

## Демо без регистрации

Точка входа для показа: `/demo` в web зовёт `POST /v1/demo/enter`, получает обычную сессию better-auth и уходит на дашборд — смотрящий внутри наполненного кабинета за один переход, без формы регистрации. Устройство:

| Что        | Как сделано                                                                            | Почему так                                                         |
| ---------- | -------------------------------------------------------------------------------------- | ------------------------------------------------------------------ |
| Аккаунт    | один пользователь `demo@multa.local`, создаётся штатным `signUpEmail` при первом входе | пароль-спидбамп (`DEMO_PASSWORD`), не секрет: сервер входит сам    |
| Изоляция   | обычный workspace, скоуп из токена (правило 7)                                         | демо-сессия не видит и не правит чужие данные — закреплено тестом  |
| Данные     | `apps/api/src/demo/seed.ts`, идемпотентно: полная перезапись демо-воркспейса           | повторный вход не удваивает историю                                |
| Язык       | воркспейс `locale: 'en'`, web подхватывает локаль из `/v1/me`                          | демо всегда на английском, независимо от языка продукта            |
| Наполнение | 6 периодов истории, долги/корзины/конверты/категории/цели, размены с разными спредами  | видно работающий каскад, факт, прогноз и копилку потерь на размене |
| Свежесть   | cron раз в час (`apps/api/src/demo/cron.ts`) + ручной `POST /v1/demo/reset`            | «испачканное» демо само возвращается к исходной картинке           |

Сид обязан оставлять план **несжатым**: дохода хватает на все обязательства, иначе показ открывается каскадом в нулях. Это проверяется тестом, а не глазами.

## Тестовый контур

Три уровня, каждый отвечает за своё:

| Уровень            | Где                                   | Что проверяет                                                                                                                                                               |
| ------------------ | ------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Юниты ядра         | `packages/core/src/*.test.ts`         | чистые функции и инварианты 01-domain-model (деньги, периоды, каскад, FX, парсер)                                                                                           |
| Юниты api          | `apps/api/src/**/*.test.ts`           | границы: Zod-схемы, таймзона, разбор ответов внешних сервисов                                                                                                               |
| Интеграционные api | `apps/api/test/*.test.ts`             | роуты целиком: изоляция workspace, каскад и сжатие, факт, исполнение, пересборка, чеки, размен, прогноз, демо-вход                                                          |
| Юниты web          | `apps/web/src/**/*.test.ts`           | vitest + jsdom: сборка auth-клиента, форматирование                                                                                                                         |
| Смоук прод-бандла  | `apps/web/test/prod-bundle.test.ts`   | собранный `dist/` исполняется в jsdom и монтируется; ассеты из `index.html` существуют                                                                                      |
| E2E в браузере     | `apps/web/e2e/*.spec.ts` (Playwright) | прод-сборка в Chromium на отдельной базе `multa_e2e`: вход в демо, запись траты, тема и язык, регрессии форм (#20), отсутствие горизонтального скролла на 375/768/1024/1440 |

Интеграционные тесты ходят в **настоящий Postgres**: они же проверяют миграции (`drizzle-kit`-миграции накатываются в `test/global-setup.ts` тем же механизмом, что в прод-контейнере) и SQL-агрегации плана. Запросы идут через `app.request()` — тот же путь, что у браузера, вместе с middleware и сессией better-auth; workspace_id тест подсунуть не может, ровно как и клиент.

База отдельная (`multa_test`), её имя обязано заканчиваться на `_test` — иначе `global-setup` отказывается работать: он делает `TRUNCATE`, и дев-база под ним потеряла бы данные. Адрес переопределяется через `TEST_DATABASE_URL`.

Смоук прод-бандла запускается отдельно (`pnpm --filter @multa/web test:bundle`) и требует свежего `vite build`: он импортирует именно тот файл, который уезжает на сервер, с пустым `VITE_API_URL` (как за Caddy). Причина отдельного уровня — реальная авария: логин умер падением better-auth на уровне модуля (относительный `baseURL`), а тесты по исходникам этого не видят, потому что у них своя сборка и свой env.

CI (`.github/workflows/ci.yml`) поднимает `postgres:16-alpine` сервисом, гоняет `pnpm format:check` → `pnpm typecheck` → `pnpm test` на пустой базе с нуля, затем собирает прод-web, прогоняет смоук бандла и браузерный E2E (Chromium, база `multa_e2e`, отчёт заливается артефактом при падении). Формат проверяется первым: он дешевле остальных шагов, а расхождение стиля дешевле ловить, пока диффы читаемы. Что форматтер не трогает — перечислено в `.prettierignore` (lock-файл, сборки, SQL-миграции, прототип дизайна, приватные данные).

E2E поднимает окружение сам (`apps/web/playwright.config.ts`): api на 3100 после наката миграций и `vite preview` на 4173 с `VITE_API_URL`, указывающим на этот api. База обязана отличаться от дев- и тест-базы — тесты входят в демо, а вход пересеивает демо-воркспейс. Что нашёл этот уровень в первый же прогон: гейт сессии показывал смотрящему форму регистрации вместо демо (работало только при уже существующей cookie), и топбар распирал страницу по горизонтали на 375 и 768px.

## Инвентарь API (v1)

> Хэнд-мейд, сверяется с `apps/api/src/app.ts` и `apps/api/src/routes/*`. Все ручки, кроме `/v1/health`,
> `/v1/auth/*` и `/v1/demo/*`, требуют сессии; скоуп workspace берётся из токена, клиент его не передаёт
> (правило 7) — демо-сессия здесь не исключение, она такой же ограниченный воркспейс.

| Метод и путь                                                          | Что делает                                                                                                                                                 | Спринт |
| --------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| `GET /v1/health`                                                      | живость + свежесть курсов                                                                                                                                  | 1      |
| `* /v1/auth/*`                                                        | better-auth (email+password, TOTP)                                                                                                                         | 1      |
| `GET /v1/me`                                                          | пользователь, его workspace и «сегодня» в таймзоне воркспейса (клиент дату не вычисляет)                                                                   | 1      |
| `DELETE /v1/me?confirm=<почта>`                                       | удаление аккаунта: каскадом уносит воркспейсы, членства и сессии; подтверждение — своя почта                                                               | 6      |
| `GET /v1/export/transactions.csv`                                     | выгрузка трат файлом: сумма, валюта, курс на дату, категория, заметка                                                                                      | 6      |
| `POST /v1/debts/:id/repaid`                                           | возврат по займу: уменьшает остаток и пишет приход; для обычного долга отвечает 422                                                                        | 6      |
| `POST /v1/client-errors`                                              | приём ошибок фронта в логи; текст чистится от личного (`scrubPii`), Sentry SaaS не используем                                                              | 6      |
| `GET /v1/metrics`                                                     | воронка и возврат на 7-й день — по всем воркспейсам; пересборки — по своему. Гостю демо отказ                                                              | 6      |
| `POST /v1/workspace`, `PATCH /v1/workspace`                           | создание и правка воркспейса                                                                                                                               | 1      |
| `GET /v1/workspace/settings`, `PATCH`                                 | настройки поведения: буфер темпа, порядок сжатия, горизонт медианы, курс и спред по умолчанию                                                              | 6      |
| `POST /v1/onboarding/income`                                          | ритм + источники дохода одним шагом                                                                                                                        | доход  |
| `POST /v1/onboarding/skip`                                            | пропустить обучение, войти с пустым планом                                                                                                                 | доход  |
| `GET/POST/PATCH/DELETE /v1/income-sources`                            | источники дохода                                                                                                                                           | доход  |
| `POST /v1/income-sources/:id/received`                                | подтверждение поступления: факт и курс дня выплаты                                                                                                         | 6      |
| `DELETE /v1/income-receipts/:id`                                      | отмена подтверждения: план возвращается к плановой сумме, личный курс снимается                                                                            | 6      |
| `GET /v1/plan/current`                                                | план периода: каскад, факт, сигнал burn-rate                                                                                                               | 2–4    |
| `PUT/DELETE /v1/plan/current/categories/:id`                          | бюджет категории на период                                                                                                                                 | 2      |
| `POST /v1/plan/current/items/:kind/:id/confirm`, `/skip`              | исполнение плановой строки                                                                                                                                 | 3      |
| `POST /v1/plan/current/items/:kind/:id/unconfirm`                     | снять отметку исполнения: статус в `pending`, транзакция-факт удаляется                                                                                    | 6      |
| `POST /v1/plan/current/items/goal/:id/freeze`, `/unfreeze`            | пропуск взноса в цель на период: осознанный, с записью в историю                                                                                           | 6      |
| `GET/POST /v1/plan/current/rebalance`                                 | варианты пересборки и применение                                                                                                                           | 4      |
| `GET /v1/plan/current/revisions`                                      | история правок периода: что, откуда, когда                                                                                                                 | 6      |
| `POST /v1/plan/current/revisions/:id/undo`                            | откат правки; сам откат тоже ревизия                                                                                                                       | 6      |
| `GET /v1/proposals`                                                   | предложения правок: участнику свои, владельцу все                                                                                                          | 6      |
| `POST /v1/proposals`                                                  | участник предлагает правку (владельцу отказ: он правит напрямую)                                                                                           | 6      |
| `POST /v1/proposals/:id/accept`, `/reject`                            | решение владельца; принятие выполняет ту же операцию, что правка ячейки                                                                                    | 6      |
| `GET /v1/forecast`                                                    | таймлайн: закрытие долгов, цели, риски                                                                                                                     | 4      |
| `GET /v1/analytics/categories?periods=N`                              | план против медианы факта, ряд для спарклайна, вердикт (в т.ч. «нестабильно»)                                                                              | 6      |
| `GET /v1/plan/grid?periods=N`                                         | мастер-сетка: строки статей × периоды выплат; вперёд — проекция, ничего не пишет                                                                           | 6      |
| `PUT /v1/plan/grid/cell?periods=N`                                    | правка ячейки таблицы; будущий период пишет ступень «с этой даты и далее»                                                                                  | 6      |
| `GET /v1/signals`                                                     | что требует решения сейчас: правило, severity, метрика, параметры и 1–2 действия                                                                           | 6      |
| `GET /v1/analytics/spread?months=N`                                   | сравнение провайдеров размена: средний спред, объёмы, у кого дешевле и сколько дал бы переход                                                              | 6      |
| `GET/POST/PATCH/DELETE /v1/accounts`                                  | счета: наличные, карты, накопительные; архивация вместо удаления                                                                                           | 6      |
| `GET /v1/accounts/balances`                                           | «сколько всего денег»: суммы по валютам + итог в базовой                                                                                                   | 6      |
| `GET/POST/PATCH/DELETE /v1/debts`, `/envelopes`, `/goals`, `/buckets` | обязательства; PATCH правит строку, не теряя историю (#91); у долга есть `direction`: заём (`owed_to_me`) в каскад не идёт (#94)                           | 2      |
| `GET/POST/PATCH/DELETE /v1/categories`                                | категории                                                                                                                                                  | 2      |
| `POST /v1/import/preview`                                             | что получится из файла: строки, период, сумма, категории; ничего не пишет                                                                                  | 6      |
| `POST /v1/import/plan-preview`                                        | предпросмотр ПЛАНА из таблицы «строки × периоды»: суммы по каждой строке + подсказка вида (долг/цель/конверт/категория/пропустить), ничего не пишет (#124) | 6      |
| `POST /v1/import/plan-apply`                                          | перенос плана: создаёт то, что человек выбрал в предпросмотре; повторный перенос не удваивает сущность с тем же именем (#124)                              | 6      |
| `POST /v1/import/commit`                                              | перенос пачкой: дедупликация по отпечатку строки, создание категорий                                                                                       | 6      |
| `GET /v1/import/batches`, `DELETE /v1/import/batches/:id`             | список пачек и откат импорта целиком                                                                                                                       | 6      |
| `GET/POST/DELETE /v1/transactions`                                    | факт: траты и внеплановые приходы. GET принимает `from`/`to`, `categoryId` (только своя категория, иначе 404) и `limit` (по умолчанию 200, потолок 500)    | 3      |
| `POST /v1/transactions/parse`                                         | разбор фразы: regex ядра, за ним LLM-фоллбэк                                                                                                               | 5      |
| `POST /v1/transactions/voice`                                         | голосовая заметка → Whisper → тот же разбор фразы                                                                                                          | 5      |
| `GET/POST/PATCH/DELETE /v1/recurring-items`                           | регулярные платежи вне обязательств: правило повтора, срок жизни, метка на карте                                                                           | 5      |
| `GET/POST/DELETE /v1/exchange-ops`                                    | размены, фактический курс, спред и провайдер                                                                                                               | 3      |
| `GET /v1/receipts`, `POST /v1/receipts/qr`, `/photo`, `/:id/confirm`  | чеки: QR-путь, vision-фоллбэк, подтверждение раскладки                                                                                                     | 5      |
| `GET /v1/fx/rate`                                                     | курс пары на дату                                                                                                                                          | 1      |
| `GET /v1/workspace/members`                                           | состав участников и матрица видимости (владельцу — полная, участнику — своя роль)                                                                          | 6      |
| `POST/GET /v1/workspace/invites`                                      | одноразовый код приглашения; список непринятых                                                                                                             | 6      |
| `POST /v1/workspace/invites/:code/accept`                             | принять приглашение: работает у того, у кого своего воркспейса ещё нет                                                                                     | 6      |
| `DELETE /v1/workspace/members/:id`                                    | исключить участника; доступ пропадает сразу                                                                                                                | 6      |
| `POST /v1/demo/enter`                                                 | вход в демо без регистрации: сессия + наполненный воркспейс                                                                                                | 6      |
| `POST /v1/demo/reset`                                                 | возврат демо к исходному наполнению (то же делает часовой cron)                                                                                            | 6      |
